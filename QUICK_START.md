# 🚀 Быстрый старт на VPS

## ⚡ Самый простой способ (одна команда):

```bash
curl -fsSL https://raw.githubusercontent.com/kamaero/turner-telegram-bot/main/deploy.sh | bash
```

## 📋 Или пошагово:

### 1. Подготовка VPS
```bash
# Установка Docker (Ubuntu/Debian)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезапуск сессии
exit && ssh user@your-vps-ip
```

### 2. Запуск развертывания
```bash
git clone https://github.com/kamaero/turner-telegram-bot.git
cd turner-telegram-bot
chmod +x deploy.sh
./deploy.sh
```

### 3. Настройка .env
Скрипт автоматически создаст `.env` файл. Откройте его:
```bash
nano .env
```

**Обязательно установите:**
```bash
BOT_TOKEN=1234567890:ABCDEF...  # Ваш токен от @BotFather
ADMIN_PANEL_PASSWORD=your_secure_password
DB_PASS=your_secure_db_password
DB_ROOT_PASSWORD=your_secure_root_password
```

### 4. Перезапуск с новыми настройками
```bash
docker-compose restart
```

## 🌐 Доступ к проекту:

После развертывания проект будет доступен по адресу:
```
http://your-vps-ip:8080/admin/
```

## 📊 Проверка работы:

1. **Админ-панель:** Откройте в браузере `http://IP:8080/admin/`
2. **Тест токена:** Перейдите к `http://IP:8080/admin/test_token.php`
3. **Статус контейнеров:** `docker-compose ps`
4. **Логи:** `docker-compose logs -f`

## 🔧 Что делает скрипт развертывания:

- ✅ Проверяет зависимости (Docker, Git)
- ✅ Клонирует проект с GitHub
- ✅ Создает .env файл
- ✅ Генерирует безопасные пароли
- ✅ Настраивает файрвол
- ✅ Запускает все контейнеры
- ✅ Проверяет статус
- ✅ Показывает URL для доступа

## 🆘 Если что-то пошло не так:

### Контейнеры не запускаются:
```bash
cd turner-telegram-bot
docker-compose logs  # Посмотреть ошибки
```

### Нет доступа к админ-панели:
```bash
# Проверьте порт
sudo netstat -tlnp | grep :8080

# Проверьте файрвол
sudo ufw status
```

### Проблемы с токеном:
1. Получите токен у @BotFather в Telegram
2. Проверьте формат: `1234567890:ABCDEF...`
3. Убедитесь что бот запущен и не заблокирован

## 📱 Минимальные требования к VPS:

- **RAM:** 1GB+ (рекомендуется 2GB)
- **Disk:** 10GB+
- **OS:** Ubuntu 20.04+, CentOS 8+, Debian 11+
- **Docker:** 20.10+

## 🎯 Готово!

После выполнения этих шагов ваш Telegram бот будет полностью работать на VPS с админ-панелью!

**Для подробной инструкции смотрите `VPS_DEPLOYMENT.md`** 📖
