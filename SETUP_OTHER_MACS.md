# 🍎 Настройка Turner Telegram Bot на других Mac

## 📋 Быстрая инструкция

### Шаг 1: Клонирование проекта
```bash
# Откройте Terminal на другом Mac
cd ~/Desktop  # или в любую удобную папку

# Клонируйте ваш форк проекта
git clone git@github.com:kamaero/turner-telegram-bot.git

# Перейдите в папку проекта
cd turner-telegram-bot
```

### Шаг 2: Установка зависимостей
```bash
# Установите Python зависимости
pip3 install -r requirements.txt

# Если pip3 нет, установите Python:
# brew install python
```

### Шаг 3: Настройка Zed Dev
```bash
# Запустите скрипт настройки Zed Dev
./sync_zed.sh

# Скрипт автоматически создаст:
# - Конфигурацию Zed Dev (.zed/)
# - Тестовый скрипт (test_db.py)
# - Руководство (README_ZED.md)
# - Обновит .gitignore
```

### Шаг 4: Настройка окружения
```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл (опционально для локальной разработки):
nano .env
```

### Шаг 5: Откройте проект в Zed Dev
```bash
# Откройте проект в Zed Dev
zed .
```

## 🛠️ Подробная инструкция

### 1. Предварительные требования

#### Установите на других Mac:
- **Git**: `brew install git`
- **Python 3**: `brew install python`
- **Docker Desktop**: https://www.docker.com/products/docker-desktop/
- **Zed Dev**: https://zed.dev/

#### Проверьте установку:
```bash
git --version
python3 --version
docker --version
zed --version
```

### 2. Настройка SSH ключей для GitHub

Если на других Mac нет SSH ключей для GitHub:

```bash
# 1. Сгенерируйте SSH ключ
ssh-keygen -t ed25519 -C "ваш_email@example.com"

# 2. Добавьте ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 3. Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub

# 4. Добавьте ключ на GitHub:
# - Откройте https://github.com/settings/keys
# - Нажмите "New SSH key"
# - Вставьте ключ
```

### 3. Настройка Git

```bash
# Настройте имя и email
git config --global user.name "Ваше Имя"
git config --global user.email "ваш_email@example.com"

# Настройте безопасные директории (если нужно)
git config --global --add safe.directory /путь/к/turner-telegram-bot
```

### 4. Альтернативный способ: Ручная настройка

Если скрипт `sync_zed.sh` не работает, настройте вручную:

```bash
# 1. Создайте директорию .zed
mkdir -p .zed

# 2. Скопируйте конфигурацию из основного Mac:
# - .zed/settings.json
# - .zed/tasks.json  
# - .zed/launch.json
# - README_ZED.md
# - test_db.py

# 3. Добавьте в .gitignore:
echo ".zed/" >> .gitignore
echo "test_db.py" >> .gitignore
```

### 5. Проверка работоспособности

```bash
# 1. Проверьте синтаксис Python файлов
python3 -m py_compile bot.py config.py database.py

# 2. Запустите тестовый скрипт
python3 test_db.py

# 3. Проверьте Docker
docker-compose config

# 4. Проверьте Git
git status
git remote -v
```

## 🚀 Рабочий процесс на нескольких Mac

### Синхронизация изменений между Mac

#### С Mac 1 (основной) → на Mac 2 и Mac 3:
```bash
# На Mac 1:
git add .
git commit -m "Описание изменений"
git push origin main

# На Mac 2 и Mac 3:
git pull origin main
```

#### Если были локальные изменения на Mac 2/Mac 3:
```bash
# Сохраните изменения
git stash

# Получите обновления
git pull origin main

# Верните изменения
git stash pop
```

### Использование Zed Dev на всех Mac

#### Общие задачи (Cmd+Shift+P → "Tasks: Run Task"):
- **Run Bot** - Запуск бота
- **Install Dependencies** - Установка зависимостей
- **Docker Compose Up** - Запуск контейнеров
- **Git Pull from Origin** - Получение обновлений
- **Git Push to Origin** - Отправка изменений

#### Отладка:
- `F5` - Запуск отладки
- `F9` - Точки останова
- `F10`/`F11` - Шаг через/внутрь

## 📁 Структура проекта после настройки

```
turner-telegram-bot/
├── .zed/                    # Конфигурация Zed Dev
│   ├── settings.json       # Настройки редактора
│   ├── tasks.json          # Задачи для быстрого доступа
│   └── launch.json         # Конфигурации отладки
├── sync_zed.sh             # Скрипт настройки (этот файл)
├── README_ZED.md           # Руководство по Zed Dev
├── test_db.py              # Тестовый скрипт
├── .env.example            # Пример конфигурации
├── .gitignore              # Исключения Git
└── ... остальные файлы проекта
```

## 🔧 Устранение неполадок

### Проблема: Git не клонирует
```bash
# Проверьте SSH ключи
ssh -T git@github.com

# Если ошибка, добавьте ключ в ssh-agent
ssh-add ~/.ssh/id_ed25519
```

### Проблема: Нет доступа к pip3
```bash
# Установите Python через brew
brew install python

# Или используйте системный Python
python3 -m ensurepip --upgrade
```

### Проблема: Zed Dev не открывает проект
```bash
# Переустановите Zed Dev
# Или откройте через терминал
zed /полный/путь/к/turner-telegram-bot
```

### Проблема: Docker не работает
```bash
# Запустите Docker Desktop
open -a Docker

# Или установите через brew
brew install --cask docker
```

## 🎯 Полезные команды для всех Mac

### Git команды:
```bash
git co          # checkout (алиас)
git st          # status (алиас)
git lg          # Красивый лог (алиас)
git push origin main    # Отправить изменения
git pull origin main    # Получить изменения
```

### Docker команды:
```bash
docker-compose up -d    # Запустить контейнеры
docker-compose down     # Остановить контейнеры
docker-compose logs -f  # Смотреть логи
```

### Python команды:
```bash
python3 bot.py          # Запустить бота
python3 test_db.py      # Запустить тесты
pip3 install -r requirements.txt  # Установить зависимости
```

## 📞 Поддержка

### Если что-то не работает:
1. **Проверьте версии** всех программ
2. **Прочитайте README_ZED.md** - полное руководство
3. **Запустите тесты** - `python3 test_db.py`
4. **Проверьте логи** - смотрите сообщения об ошибках

### Полезные ссылки:
- **GitHub репозиторий**: https://github.com/kamaero/turner-telegram-bot
- **Zed Dev документация**: https://zed.dev/docs
- **Docker документация**: https://docs.docker.com/

### Контакты для помощи:
- Основной Mac: Где настроен проект
- VPS: 31.31.207.220 (уже настроен)
- Домены: motorist-ufa.ru, motorist-ufa.online

---

## ✅ Чеклист настройки для каждого Mac

- [ ] Установлен Git (`git --version`)
- [ ] Установлен Python 3 (`python3 --version`)
- [ ] Установлен Docker Desktop
- [ ] Установлен Zed Dev
- [ ] Настроены SSH ключи для GitHub
- [ ] Проект клонирован
- [ ] Запущен `./sync_zed.sh`
- [ ] Настроен `.env` файл (опционально)
- [ ] Проект открыт в Zed Dev
- [ ] Запущен тест `python3 test_db.py`

**🎉 Готово! Все Mac синхронизированы и готовы к работе!**