# 🚨 Важно: Развертывание при существующем сайте в /var/www

## 📁 Ваша текущая структура:
```
/var/www/
├── html/                    # Основной сайт
├── motorist-ufa.ru/         # Ваш сайт
└── .git/                    # Git репозиторий
```

## ⚠️ Проблема:
Docker пытается монтировать админку в `/var/www/html`, что может повредить существующий сайт.

## ✅ Рекомендуемые решения:

### 🎯 Вариант 1: Развернуть в /opt/turner-bot (ЛУЧШИЙ)

**Преимущества:**
- ✅ Полная изоляция от существующих сайтов
- ✅ Безопасно и чисто
- ✅ Легко управлять
- ✅ Автозапуск через systemd

**Выполнение:**
```bash
# 1. Скачайте и запустите скрипт
cd /tmp
curl -fsSL https://raw.githubusercontent.com/kamaero/turner-telegram-bot/main/deploy_opt.sh -o deploy_opt.sh
chmod +x deploy_opt.sh
./deploy_opt.sh

# 2. Настройте .env
nano /opt/turner-bot/.env

# 3. Перезапустите
cd /opt/turner-bot
docker-compose restart
```

**Результат:**
- Проект в: `/opt/turner-bot/`
- Доступ: `http://IP:8081/admin/`
- Автозапуск: через systemd

### 🔄 Вариант 2: Развернуть в /var/www (АЛЬТЕРНАТИВНЫЙ)

**Если обязательно нужно в /var/www:**

```bash
# 1. Перейдите в директорию
cd /var/www

# 2. Создайте подпапку для проекта
mkdir turner-bot
cd turner-bot

# 3. Клонируйте проект
git clone https://github.com/kamaero/turner-telegram-bot.git .

# 4. Используйте альтернативный docker-compose
docker-compose -f docker-compose.alt.yml up -d
```

**Особенности:**
- Админка монтируется в `/var/www/html/turner-admin`
- Доступ: `http://IP:8081/turner-admin/`
- Не затрагивает существующие сайты

### 📋 Сравнение вариантов:

| Параметр | Вариант 1 (/opt) | Вариант 2 (/var/www) |
|----------|------------------|---------------------|
| Изоляция | ✅ Полная | ⚠️ Частичная |
| Безопасность | ✅ Высокая | ⚠️ Средняя |
| Управление | ✅ Легкое | ⚠️ Сложнее |
| Автозапуск | ✅ Systemd | ⚠️ Ручной |
| URL | `:8081/admin/` | `:8081/turner-admin/` |

## 🚨 Чего НЕ делать:

❌ **НЕ разворачивайте в /var/www/html** - заменит существующий сайт  
❌ **НЕ используйте порт 80** - конфликт с основным сайтом  
❌ **НЕ монтируйте в корень /var/www/html** - удалит существующие файлы  

## 🔧 Настройка после развертывания:

### 1. Настройте .env:
```bash
# Для варианта 1
nano /opt/turner-bot/.env

# Для варианта 2  
nano /var/www/turner-bot/.env
```

**Обязательно установите:**
```bash
BOT_TOKEN=1234567890:ABCDEF...  # Токен от @BotFather
ADMIN_PANEL_PASSWORD=your_secure_password
DB_PASS=your_secure_db_password
DB_ROOT_PASSWORD=your_secure_root_password
```

### 2. Проверьте работу:
```bash
# Статус контейнеров
docker-compose ps

# Логи при проблемах
docker-compose logs -f

# Доступ в браузере
http://IP:8081/admin/
```

### 3. Настройте автозапуск (только для варианта 1):
```bash
sudo cp /opt/turner-bot/turner-bot.service /etc/systemd/system/
sudo systemctl enable turner-bot
sudo systemctl start turner-bot
```

## 🎯 Рекомендация:

**Используйте Вариант 1 (/opt)** - это самый безопасный и правильный способ развертывания Docker приложений на сервере с существующими сайтами.

**Ваши существующие сайты останутся нетронутыми!** ✅
