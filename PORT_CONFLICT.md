# 🚨 Важно: Конфликт с существующим сайтом

## ⚠️ Проблема:
Docker монтирует папку `admin/` в `/var/www/html` и заменяет все файлы существующего сайта.

## ✅ Решения:

### Вариант 1: Изменить порт (рекомендуется)
Админ-панель будет доступна на порту 8081:
```
http://your-ip:8081/admin/
```

### Вариант 2: Изменить путь монтирования
Если вы хотите сохранить порт 8080:

```yaml
web-admin:
  build:
    context: .
    dockerfile: Dockerfile-web-admin
  restart: always
  ports:
    - "${WEB_PORT:-8080}:80"
  volumes:
    - ./admin:/var/www/html/admin  # Монтируем в подпапку
    - ./init.sql:/docker-entrypoint-initdb.d/init.sql
```

Тогда доступ будет по адресу:
```
http://your-ip:8080/admin/
```

### Вариант 3: Отдельный домен/поддомен
Настройте Nginx прокси:
```nginx
server {
    listen 80;
    server_name admin.your-domain.com;
    
    location / {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🔄 Что я изменил:
- Порт по умолчанию изменен с 8080 на 8081
- Теперь админ-панель не будет конфликтовать с вашим сайтом

## 🌐 Новые URL доступа:
- **Админ-панель:** `http://your-ip:8081/admin/`
- **Тест токена:** `http://your-ip:8081/admin/test_token.php`

## 📋 Если хотите вернуть порт 8080:
1. Измените в `.env`: `WEB_PORT=8080`
2. Или используйте Вариант 2 выше

## 🛡️ Безопасность:
- Используйте разные порты для разных сервисов
- Настройте файрвал для каждого порта
- Рассмотрите использование HTTPS для продакшена
