# 🚀 Развертывание на VPS в Docker

## 📦 Файлы для развертывания (ОБЯЗАТЕЛЬНЫЕ):

### Docker конфигурация:
- `docker-compose.yml` - основная конфигурация сервисов
- `Dockerfile` - сборка Python бота
- `Dockerfile-web-admin` - сборка PHP админки
- `docker-entrypoint.sh` - скрипт инициализации

### Код проекта:
- `bot.py` - главный файл бота
- `config.py` - конфигурация Python
- `database.py` - работа с БД
- `requirements.txt` - Python зависимости

### PHP админка:
- `admin/admin.php` - основная страница админки
- `admin/php_config.php` - конфигурация PHP
- `admin/test_token.php` - тест токена
- `admin/index.php` - перенаправление

### База данных:
- `schema.sql` - структура БД

### Конфигурация:
- `.env.example` - шаблон конфигурации
- `.gitignore` - исключения Git

## ❌ Лишние файлы (удалены):

### Разработка:
- `.idea/` - настройки PyCharm
- `.gigaide/` - настройки IDE
- `venv/` - виртуальное окружение Python
- `.DS_Store` - системные файлы macOS

### Временные:
- `admin.py` - альтернативный админ (не нужен)
- `install.sh` - скрипт установки (не нужен в Docker)
- `add_bot_token.sql` - временный SQL (включен в schema.sql)
- `ENV_SETUP.md` - документация (не нужна на сервере)

## 🎯 Итоговая структура:

```
turner-telegram-bot/
├── docker-compose.yml
├── Dockerfile
├── Dockerfile-web-admin
├── docker-entrypoint.sh
├── bot.py
├── config.py
├── database.py
├── requirements.txt
├── schema.sql
├── .env.example
├── .gitignore
└── admin/
    ├── admin.php
    ├── php_config.php
    ├── test_token.php
    └── index.php
```

## 📋 Размер после очистки: ~2MB (было ~50MB)

## 🚀 Команды развертывания:

```bash
# 1. Клонирование
git clone <repository>
cd turner-telegram-bot

# 2. Настройка
cp .env.example .env
nano .env  # отредактировать

# 3. Запуск
docker-compose up -d

# 4. Проверка
docker-compose ps
```

## ✅ Преимущества чистой структуры:

- 🚀 Быстрое развертывание
- 💾 Меньше места на диске
- 🔒 Безопасность (без лишних файлов)
- 🎯 Простота поддержки
- 🐳 Оптимизированный Docker образ
